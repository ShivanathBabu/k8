apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    purpose: deployment-demo 
    project: roboshop
spec:
  selector:
    purpose: deployment-demo 
    project: roboshop
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.service.targetPort }}