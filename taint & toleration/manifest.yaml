apiVersion: v1
kind: Pod
metadata:
  name: catalogue
  labels:
    project: roboshop
    tier: app
spec:
  containers:
    - name: nginx
      image: nginx
  nodeSelector:
    disktype: ssd
  tolerations:
    - key: key
      operator: "Equal"
      value: "value1"
      effect: "NoSchedule"